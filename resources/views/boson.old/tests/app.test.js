import { describe, it, expect } from 'vitest';
import * as moduleUnderTest from '@/app.js';

describe('app.js', () => {
  it('should have placeholder test', () => {
    expect(moduleUnderTest).toBeDefined();
  });
});
