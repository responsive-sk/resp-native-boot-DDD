import { describe, it, expect } from 'vitest';
import * as moduleUnderTest from '@/application/use-cases/CreateArticle.js';

describe('application/use-cases/CreateArticle.js', () => {
  it('should have placeholder test', () => {
    expect(moduleUnderTest).toBeDefined();
  });
});
